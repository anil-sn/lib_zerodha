"""lib_zerodha: A focused Kite Connect integration library.

This library provides a clean interface to Zerodha's Kite Connect API
with optimized data structures for processing.

Author: Anil Kumar S N
Version: 1.0.0
Date: January 2026
"""

from .kite_client import KiteClient
from .data_models import (
    Quote, OHLC, DepthItem, Instrument, Position,
    Order, Trade, Holding, MarginInfo, Portfolio,
    HistoricalData, GTTTrigger, MFHolding, FOInstrument, OptionChain
)
from .websocket_client import KiteWebSocketClient
from .data_processor import DataProcessor
from .database import TradingDatabase
from .realtime import ZerodhaWebSocketManager, KiteTokenWatcher
from .candle_store import InMemoryCandleStore, LocalCandleDatabase

__version__ = "1.0.0"
__all__ = [
    "KiteClient", 
    "KiteWebSocketClient",
    "DataProcessor",
    "TradingDatabase",
    "ZerodhaWebSocketManager",
    "KiteTokenWatcher", 
    "InMemoryCandleStore",
    "LocalCandleDatabase",
    "Quote", "OHLC", "DepthItem", "Instrument", 
    "Position", "Order", "Trade", "Holding", 
    "MarginInfo", "Portfolio", "HistoricalData",
    "GTTTrigger", "MFHolding", "FOInstrument", "OptionChain"
]
